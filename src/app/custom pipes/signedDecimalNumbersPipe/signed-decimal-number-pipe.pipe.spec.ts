import { SignedDecimalNumberPipePipe } from './signed-decimal-number-pipe.pipe';

describe('SignedDecimalNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SignedDecimalNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
